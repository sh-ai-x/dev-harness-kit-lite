# step<N>.md — <step title>

<!-- Generated by /dev-kit-lite:plan. Edit via /dev-kit-lite:plan-update or /dev-kit-lite:reassign. -->

## Role (mandatory — L5-R non-overlap)

- **Owner**: <person-name>     # e.g. Alice Kim
- **Discipline**: <FE | BE | Design | PM>
- **Branch**: feat/<scope>-<owner>-<slug>   # e.g. feat/web-alice-checkout
- **Worktree**: .worktrees/<scope>-<owner>  # e.g. .worktrees/web-alice
- **Owns**: <directories / files>            # e.g. apps/web/checkout/, components/CheckoutForm.tsx
- **Reads first**: <other step docs or spec links>
- **Hands off to**: <next step doc or role>
- **Coordination slot**: <time, e.g. "every 30 min sync with PM">

### Pre-flight check (must pass before starting)

- [ ] Worktree created from `origin/main` (`git worktree add -B <branch> <worktree> origin/main`)
- [ ] `git status` clean on main checkout
- [ ] **Owner not already in-flight on another step** (L5-R; checked against `phases/<name>/owners.json`)
- [ ] **No `Owns:` path conflicts with another owner's `path_locks`** (L5-R)
- [ ] PM has approved step start (recorded in `.dev-kit/session-log.md`)

## Status

`pending` | `in_progress` | `completed` | `error` | `blocked`

## Task

<2-5 sentences describing what to build/change>

## Acceptance Criteria

- <AC from PRD §3, mapped to this step>

## Verification & Status Update

After completing, run:
```bash
/dev-kit-lite:build-tdd            # RED → GREEN → REFACTOR
/dev-kit-lite:build-verify <N>    # L3 evidence gate
```

Then update this file's `## Status` line to `completed`.

## Don't

- ❌ Edit files outside `Owns:` paths
- ❌ Skip RED phase (L1)
- ❌ Skip PM sign-off (L5-R)
- ❌ Push to main (L2)
- ❌ Edit this file's `## Role` section without going through `/dev-kit-lite:reassign`
